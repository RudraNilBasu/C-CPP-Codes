#include <stdio.h>
#include <iostream>
#include <math.h>

#define MOD 998244353

using namespace std;

int main()
{
#ifdef LOCAL
        // use it via g++ c.cpp -DLOCAL
        freopen("inp.txt", "r", stdin);
#endif
        int a, b, c;
        cin >> a >> b >> c;

        return 0;
}
